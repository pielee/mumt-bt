scenario:
  environment: scenarios.mumt

agent:
  namespaces: "/F16_UAV1"            # 리더 UAV
  behavior_tree_xml: "leader_bt.xml" # 이륙 + 고도 1000 유지 직진

bt_runner:
  bt_tick_rate: 10.0
  bt_visualiser:
    enabled: True
    screen_width: 600
    screen_height: 600
  profiling_mode: False
