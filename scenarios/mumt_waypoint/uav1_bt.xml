<?xml version="1.0" encoding="UTF-8"?>
<root BTCPP_format="4">
  <BehaviorTree ID="mumt-waypoint">

    <!-- ===== F16_UAV1 : direct 헤딩/고도 조준으로 웨이포인트 추종 =====
         Takeoff 없음 — 첫 웨이포인트(동쪽 3km+1000m)를 조준해 지상에서부터 이륙·상승.
         guidance_mode="direct"(heading/altitude/target_speed) — 인엔진 3계층
         제어스택(FixedWingGuidance→F16CommandController) 구동. -->
    <ReactiveSequence>
      <GatherOwnState own_name="F16_UAV1"/>
      <FollowWaypoint own_name="F16_UAV1" cruise_speed_mps="220" accept_radius_m="400" loop="True"/>
    </ReactiveSequence>

  </BehaviorTree>
</root>
