scenario:
  environment: scenarios.mumt_formation_follow

agent:
  namespaces: "/F16_UAV1"            # 리더(M_F16) 편대 추종 윙맨 (인엔진 3계층 유도)
  behavior_tree_xml: "uav1_bt.xml"

bt_runner:
  bt_tick_rate: 10.0
  bt_visualiser:
    enabled: True
    screen_width: 600
    screen_height: 600
  profiling_mode: False
